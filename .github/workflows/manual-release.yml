name: Legacy Manual Release (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain supported production release paths
        shell: bash
        run: |
          echo "This legacy Vultr/self-hosted release workflow is retired." >&2
          echo "Use Deploy Frontend Production for Vercel and Deploy Backend to Cloud.ru for backend releases." >&2
          exit 1
