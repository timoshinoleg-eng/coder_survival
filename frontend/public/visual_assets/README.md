# visual_assets

Runtime art served as static files (public/).

- `first_pack/*_keyart_780.jpg` - event card backgrounds for RandomEventToast,
  780px height, JPEG q82. Originals in the team Drive (Visual Assets/first_pack).
- Avatar states (hero_coder_*.png) live in `src/assets/characters/` and are
  bundled via Vite imports in BootScene - do not duplicate them here.

Do NOT commit multi-MB originals; runtime versions only.