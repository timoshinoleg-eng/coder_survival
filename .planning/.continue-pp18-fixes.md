---
context: pp18-prestige-remaining-fixes
status: in-progress
last_updated: 2026-05-31
---

# CONTINUE — PP-18 Prestige: оставшиеся backend/economy fixes

ROOT = `C:\Users\Имярек\Downloads\Coder Survival\coder_survival_repo\coder_survival_fresh`
Не трогать: frontend/*, vercel.json, scripts/*.ps1, .env, YC/DNS/deploy.

## УЖЕ СДЕЛАНО (этой сессией)
- Перечитаны все файлы. P0 из старого аудита уже починены ранее:
  commits_total читается из `progression` (не users); execute сбрасывает
  active_effects/generator_state/event_state; фантомных колонок/таблиц нет;
  `resolveLevelState` уже вызывает `applyPrestigeBonuses` (drift по issue #3 закрыт).
- **Внесена 1 правка:** `backend/src/config/balance.js` — в `PRESTIGE` добавлено
  `MAX_PRESTIGE_LEVEL: 20` (после THRESHOLD_XP). Это config-cap для issue #9.

## ОСТАЛОСЬ (точные правки)

### issue #1 — `backend/src/utils/prestige.js` `computePrestige`
Сейчас: `return Math.floor(Math.sqrt(commitsTotal / 10));`
Заменить на safe-версию:
```js
export function computePrestige(commitsTotal) {
  const n = Number(commitsTotal);
  if (!Number.isFinite(n) || n <= 0) return 0;
  return Math.floor(Math.sqrt(n / 10));
}
```
(neg/NaN/Infinity → 0; 12.5→1, MAX_SAFE_INTEGER→finite сохраняются.)

### issue #2 + #9 — `backend/src/utils/prestige.js` `applyPrestigeBonuses`
Убрать ранний `if (p <= 0) return levelState;`. Сделать always-consistent + cap:
```js
export function applyPrestigeBonuses(levelState, prestigeLevel, shopPurchases = {}) {
  const raw = Number(prestigeLevel);
  const safe = Number.isFinite(raw) ? Math.max(0, raw) : 0;
  const p = Math.min(PRESTIGE.MAX_PRESTIGE_LEVEL, safe);
  return {
    ...levelState,
    commitsPerTap: levelState.commitsPerTap * (1 + PRESTIGE.BONUSES.TAP_MULT_PER_LEVEL * p),
    maxEnergy: levelState.maxEnergy + PRESTIGE.BONUSES.MAX_ENERGY_ADD_PER_LEVEL * p,
    critChanceAdd: PRESTIGE.BONUSES.CRIT_CHANCE_ADD_PER_LEVEL * p,
    energyRecoveryMult: 1 + PRESTIGE.BONUSES.ENERGY_RECOVERY_MULT_PER_LEVEL * p,
    depressionResistanceMult: Math.max(0, 1 - PRESTIGE.BONUSES.DEPRESSION_RESISTANCE_PER_LEVEL * p),
    prestigeLevel: p,
  };
}
```
(`PRESTIGE` уже импортируется в этом файле.)

### issue #9 — `backend/src/utils/vnext.js` `resolveLevelState`
Строка ~83: `prestigeLevel: p,` → `prestigeLevel: bonused.prestigeLevel,`
(чтобы cap отражался в одном источнике; остальное уже через applyPrestigeBonuses.)

### issue #4 — `backend/src/routes/tap.js` (строки ~142-143)
Удалить:
```js
const prestigeLevel = Number(levelBefore.record.prestige_level ?? 0);
const prestigeRecoveryMult = 1 + 0.05 * prestigeLevel;
```
Заменить на:
```js
const prestigeRecoveryMult = levelBefore.resolved.energyRecoveryMult || 1;
```
(`prestigeLevel` больше нигде в файле не используется — проверено.)

### issue #5 — `backend/src/utils/tap.js` `calculateDepressionDelta` (последняя строка)
`return delta * depressionMultiplier;` → `return Math.max(0, delta * depressionMultiplier);`

### issue #6 — `backend/src/routes/prestige.js` preview `willReset` (строка ~76)
Заменить массив на реальные поля:
```js
willReset: ['xp_total', 'tier', 'commits_current', 'energy', 'generator_state', 'active_effects', 'event_state'],
```
Опционально (display-cap, чтобы UI совпадал с capped-геймплеем):
- preview строка ~70 tap mult detail: использовать `Math.min(PRESTIGE.MAX_PRESTIGE_LEVEL, prestigeLevel + 1)`.
- execute bonuses (строки ~175-181): `const effLvl = Math.min(PRESTIGE.MAX_PRESTIGE_LEVEL, newPrestigeLevel);` и считать bonuses от effLvl.

### issue #7 — internalObservation.js
БЕЗ ИЗМЕНЕНИЙ. Уже корректно: строка ~1214 `COALESCE(pl.prestige_level, 0) AS prestige_level`,
строка ~1235 передаёт в `resolveLevelState`. Только подтвердить в отчёте.

### issue #8 — currency formula (PRODUCT DECISION — РЕШЕНО: оставляем как есть)
НЕ вводить `commits_total_at_last_prestige`, миграция 045 НЕ нужна.
Обоснование: спека PP-18 §2 прямо говорит «SQRT diminishing returns относительно
накопленного» = формула от lifetime `commits_total` и есть задуманное поведение
(кривая sqrt сглаживается, а не per-prestige дельта). Плюс повторный престиж
гейтится повторным гриндом 3100 XP. Зафиксировать это в отчёте как осознанное решение.

## ТЕСТЫ — `backend/tests/pp18.prestige.unit.test.js` (надо обновить 3 теста)
Эти тесты сейчас закрепляют СТАРОЕ (багованное) поведение и будут падать:
1. ~стр.39-42 `handles negative input (returns NaN)` → ожидать `0` вместо NaN.
   Переименовать в `handles negative input (returns 0)`.
2. ~стр.64-73 `p=0 ... prestigeLevel is undefined` → ожидать `result.prestigeLevel === 0`.
3. ~стр.275-282 `depressionMultiplier<0 produces negative gain` → ожидать `0` (clamped),
   переименовать соответственно.
Добавить (опц.): computePrestige(Infinity)→0, computePrestige(NaN)→0; cap-тест applyPrestigeBonuses(p=100) использует <=20 уровней.
ВАЖНО: cap=20 выбран так, чтобы p=20/p=30 тесты depressionResist→0 остались зелёными (любой cap≥20). НЕ опускать ниже 20.

## ПОСЛЕ ПРАВОК ЗАПУСТИТЬ
```
npm --prefix backend test -- --runInBand backend/tests/pp18.prestige.unit.test.js
node --check backend/src/routes/prestige.js
node --check backend/src/utils/prestige.js
node --check backend/src/utils/vnext.js
node --check backend/src/utils/tap.js
```

## ИТОГОВЫЙ ОТЧЁТ ДОЛЖЕН СОДЕРЖАТЬ
- что изменено по файлам; какие bugs закрыты (issues 1,2,3✓ранее,4,5,6,9);
- product decision по #8 (оставлено, с обоснованием выше);
- #7 — без изменений (подтверждение);
- результаты 5 команд выше.
